version: '3.8'

services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: cognito-sqlserver
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=YourStrong@Pass123
      - MSSQL_PID=Developer
    ports:
      - "11433:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql
    healthcheck:
      test: /opt/mssql-tools/bin/sqlcmd -S localhost -U sa -P YourStrong@Pass123 -Q "SELECT 1" -b -o /dev/null
      interval: 10s
      timeout: 3s
      retries: 10
      start_period: 10s
    networks:
      - cognito-network

  cognito-api:
    build:
      context: .
      dockerfile: src/Modules/Cognito/AuthSamples.Modules.Cognito.API/Dockerfile
    container_name: cognito-api
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__CognitoDatabase=Server=sqlserver,1433;Database=AuthSamplesDb;User Id=sa;Password=YourStrong@Pass123;TrustServerCertificate=True;MultipleActiveResultSets=true
      - CognitoSettings__UserPoolId=${COGNITO_USER_POOL_ID}
      - CognitoSettings__ClientId=${COGNITO_CLIENT_ID}
      - CognitoSettings__ClientSecret=${COGNITO_CLIENT_SECRET}
      - CognitoSettings__Region=${COGNITO_REGION}
    ports:
      - "5000:8080"
    volumes:
      - ./logs:/app/logs
    depends_on:
      sqlserver:
        condition: service_healthy
    networks:
      - cognito-network
    restart: unless-stopped

volumes:
  sqlserver_data:
    driver: local

networks:
  cognito-network:
    driver: bridge
