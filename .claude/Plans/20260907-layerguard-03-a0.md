# LayerGuard 03-A0 Core Bootstrap implementation

## Scope

Implement Phase 0–4 of `docs/architecture/review/plans/03-layerguard-alignment.md` before Gate
01–05 work. Do not bind Gate-owned final catalogs, provider graph, runtime role matrix, data
classification policy, or shared primitive allowlists; those remain 03-A1 work.

## Work

1. Capture the legacy B0 scan and explicit capability gaps.
2. Extend LayerGuard with ownership-aware project roles, provider/adapter boundaries, source and
   declaration policies, context/payload checks, cycle checks, and fail-closed configuration.
3. Add exact migration baseline/waiver validation and a unified local/CI entry point.
4. Add positive and negative fixtures for deterministic A0 rules and preserve existing tests.
5. Generate B0.5, document the rule-to-evidence mapping, update plan status, run tests and the
   unified gate, then commit and push only 03-A0 changes.

## Acceptance

- All existing and A0 fixture tests pass.
- B0.5 reports zero new and zero stale findings against its reviewed baseline.
- CI uses the same command as local execution.
- Phase 0–4 and LG-BOOTSTRAP are marked complete with evidence links; 03-A1 remains unchecked.
