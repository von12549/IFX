{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://ifx.local/v4/web-spike/session.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["formatVersion", "status", "authority", "allowedCommand", "allowedStages", "allowedProfiles", "roots"],
  "properties": {
    "formatVersion": { "const": 1 },
    "status": { "const": "ready" },
    "authority": { "const": "v4-host" },
    "allowedCommand": { "const": "stage.run" },
    "allowedStages": {
      "type": "array",
      "const": ["bootstrap", "analysis", "pre", "post"]
    },
    "allowedProfiles": {
      "type": "array",
      "const": ["synthetic_profile"]
    },
    "roots": {
      "type": "array",
      "minItems": 4,
      "maxItems": 4,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "path", "access"],
        "properties": {
          "name": { "enum": ["PackageRoot", "TargetRoot", "StateRoot", "EvidenceRoot"] },
          "path": { "type": "string", "minLength": 1 },
          "access": { "enum": ["immutable", "read-only", "v4-owned mutable"] }
        }
      }
    }
  }
}
