# IFX policy binding fixture

A minimal synthetic IFX project tree for the IFX policy binding tests (Plan 06 P6.4, D29): a module Domain, its
Application, a Composition project and the API host, named the way the IFX policy binds project roles. It exists so the
IFX binding tests keep their own fixture in this package and depend on no V3 test data.
