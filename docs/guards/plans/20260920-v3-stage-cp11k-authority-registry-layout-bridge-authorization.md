# Authorize CP11k authority registry layout bridge

This authorization-only checkpoint pre-authorizes the exact trusted-component tuples of the authority registry layout bridge. It changes no runtime implementation, policy registry or domain-authority registry.
