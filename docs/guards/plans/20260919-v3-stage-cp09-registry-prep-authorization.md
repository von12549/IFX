# CP09 registry prep authorization

This authorization checkpoint binds the temporary analysis-directory exclusions to raw candidate `9bffcdf3` on CP08 base `9ab7d74a`.

It authorizes exactly one trusted component change and one semantic policy/config change. The following change checkpoint must delete both records while applying the tuple-bound candidate; unused or replayed records remain invalid.
