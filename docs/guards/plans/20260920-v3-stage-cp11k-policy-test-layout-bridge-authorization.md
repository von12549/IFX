# Authorize CP11k policy test layout bridge

This authorization-only checkpoint binds the single base-owned package-test tuple of the policy fixture layout bridge. It changes no test implementation, runtime implementation or policy.
