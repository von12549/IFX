# CP11a maintenance relocation authorization

This authorization-only checkpoint pre-authorizes exact candidate `e545a2c2` against verified two-bridge base `91f8041f`. It changes no production runner, authority, projection, workflow or required check.

Two `move` records bind the old and new Git tuples of the policy projection and history manifest maintenance tools. The `change-trusted-base` record covers four components and ten exact paths while requiring unchanged projection bytes, history-integrity verdicts, required checks and workflow behavior. Its only allowed `Validate` difference is the generated-documentation staleness introduced by these authorization-only Plan files. The `weaken-policy` record binds the three zero-comparator manifest/configuration changes to exact hashes and JSON pointers.

The CP11a change must delete all four records in the same diff. No legacy internal wrapper, CI activation or remote setting change is authorized.
