# CP11f Specialized relocation bridge authorization

This authorization-only checkpoint binds the exact base-owned contract, manifest and target-root-separation test tuples for the Specialized relocation bridge. The candidate must delete and consume the record in the same protected diff; this checkpoint changes no production entry point or Specialized implementation.
