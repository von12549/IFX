# CP11m — Authorize the refactor baseline policy exclusion bridge

This authorization checkpoint records the exact `tcb.manifest` and `shared/policy-config.json` changes prepared by the policy exclusion bridge. The bridge changes no existing policy input and excludes only the currently empty future refactor-baseline evidence prefix.
