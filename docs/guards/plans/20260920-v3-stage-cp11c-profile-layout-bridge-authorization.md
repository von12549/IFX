# CP11c profile layout bridge authorization

This authorization-only checkpoint permits the exact trusted-base bridge prepared for CP11c. The authorization binds all changed tuples in the generic V3 runner, its new layout contract/resolver, the additive test coverage and the trusted-component manifest.

The candidate must consume the authorization in the same protected diff. No profile authority relocation, IFX caller switch, CI activation or behavior weakening is authorized.
