# CP11c profile repository-root bridge authorization

This authorization-only checkpoint permits the exact trusted-base changes prepared for the CP11c profile repository-root bridge. It binds the generic V3 resolver and entry points, the IFX facade calls, and the additive separated-root tests to the reviewed candidate tuples.

The candidate must consume the authorization in the same protected diff. No profile authority relocation, policy registry expansion, CI activation or gate weakening is authorized.
