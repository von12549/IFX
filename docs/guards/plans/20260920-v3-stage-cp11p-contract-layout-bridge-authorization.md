# Authorize the CP11p contract layout bridge

This authorization checkpoint binds the exact trusted-base engine, manifest checker, command, CI contract and base-owned test tuples of the frozen CP11p bridge candidate, plus the exact registry pointer that registers D32. It changes no implementation or contract authority and is consumed by the following bridge checkpoint.
