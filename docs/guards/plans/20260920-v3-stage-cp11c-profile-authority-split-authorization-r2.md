# CP11c profile authority split authorization (r2)

This authorization-only checkpoint permits the exact profile authority split prepared after the repository-root and policy-registry bridges. It binds the trusted-component edits, the final policy registry replacement, and five physical move groups to the reviewed candidate tree.

The candidate must consume all seven authorizations in the same protected diff. The move records preserve each source/destination tuple; the policy authorization replaces temporary directory registration with exact files and schemas; the TCB authorization covers only the callers, manifests, tests and base-owned candidate selector required by the split.
