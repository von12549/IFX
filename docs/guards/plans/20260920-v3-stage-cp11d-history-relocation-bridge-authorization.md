# CP11d Historical Integrity relocation bridge authorization

This authorization-only checkpoint binds the exact trusted-base and policy-registry bridge candidate prepared from base `72094201`. It authorizes fail-closed legacy/Post-stage layout selection in base-owned verification and the temporary destination-directory registration.

The candidate must consume both records in the same protected diff. These records do not move the Historical Integrity engine or manifest and do not change any production entry point.
