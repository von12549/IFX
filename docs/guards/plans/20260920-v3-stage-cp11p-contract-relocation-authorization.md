# Authorize the CP11p contract ownership relocation

This authorization checkpoint binds the exact 16 schema moves, seven duplicate-schema deletions, trusted-component tuples and policy/config pointers of the frozen CP11p relocation candidate. It changes no implementation or contract authority and is consumed by the following relocation checkpoint.
