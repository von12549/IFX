# CP11b V3_backup retirement authorization

This authorization-only checkpoint permits the exact P10.5 candidate prepared at `b4194d11` against the verified bridge base `ff191f84`.

The directory-level delete binds the original `V3_backup` tree ID and every one of its 41 tracked files. The orthogonal trusted-base record covers removal of the completed compatibility entry, isolated-copy input and tightened base-owned tests. The policy/config record covers only `/compatibility/entries/9` in `guard-system.json`.

The change must delete all three records in the same diff. It may not alter the canonical V3 tree, workflow activation, required checks or protection of the retired path.
