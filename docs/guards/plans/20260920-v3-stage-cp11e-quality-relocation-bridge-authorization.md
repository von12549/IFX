# CP11e Quality relocation bridge authorization

This authorization-only checkpoint binds the exact base-owned Assembly, Package and manifest test tuples for the Quality relocation bridge. The candidate must delete and consume the record in the same protected diff; this checkpoint changes no production entry point or Quality implementation.
