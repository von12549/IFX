# CP11k — Policy test layout bridge

This expand checkpoint makes the base-owned manifest negative fixture follow the already-supported legacy or Post-stage policy root. The assertion remains identical: an unregistered JSON policy file must fail manifest validation. Only the fixture location becomes layout-aware so the previous trusted base can validate the authority-registry relocation candidate after the legacy `policy/` directory disappears.
