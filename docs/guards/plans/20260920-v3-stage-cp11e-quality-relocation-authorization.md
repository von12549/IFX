# CP11e Quality relocation authorization

This authorization-only checkpoint binds the exact Quality directory move, trusted-component tuples and three semantic manifest changes. The relocation candidate must delete and consume all three records in one protected diff.
