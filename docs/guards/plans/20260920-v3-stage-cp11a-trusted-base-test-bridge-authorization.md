# CP11a trusted-base workflow fixture bridge authorization

This authorization-only checkpoint permits exact candidate `685405f9` against maintenance-bridge base `8719ae8b`. It covers only the `tcb.validation.package-tests` tuple for `Test-IFXTrustedBase.ps1`.

Production verifier, workflow and manifest behavior is unchanged. The test restores the existing unregistered-executable negative control for the current public-facade workflow while retaining the legacy fixture shape. The authorization-only Plan files create the narrow recorded `Validate` generated-documentation difference; all remaining validation checks and five verdict modes stay under parity.

The bridge change must consume this record before it becomes the base for the rebuilt maintenance relocation.

