# CP11j policy layout bridge authorization

This authorization-only checkpoint binds the nine trusted tuples and six affected TCB components for the policy-layout compatibility bridge. The bridge change consumes the record exactly once without modifying policy data or its registry.
