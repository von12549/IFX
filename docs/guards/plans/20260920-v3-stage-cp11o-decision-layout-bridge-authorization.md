# Authorize the CP11o decision-history layout bridge

This authorization checkpoint binds the exact trusted-base engine and package-test tuples of the frozen CP11o bridge candidate. It changes no implementation or decision authority and is consumed by the following bridge checkpoint.
