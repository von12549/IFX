# CP09 base compatibility authorization

This checkpoint authorizes the exact dual-layout validation bridge in raw candidate `0a01c670` on registry-prepared base `4a5a3a57`.

One `change-trusted-base` record covers four validator/test files across `tcb.manifest` and `tcb.validation.package-tests`. The following change commit must consume the record once and then become the authorization base for CP09 proper.
