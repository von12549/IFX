# CP11f Specialized contract registry prep authorization

This authorization-only checkpoint binds the exact trusted-manifest and policy-registry tuples for pre-registering the future stage-owned Specialized contract directory. The prep candidate must consume both records in the same protected diff and changes no runtime entry point or detector.
