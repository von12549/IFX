# CP11j — Policy registry move bridge

This expand checkpoint lets the previous trusted base classify a protected policy-data relocation without trusting head verdict code. The base validates the explicit head policy and authority registries against base schemas, unions reproducible head projection targets with base targets, and treats head-registered additions plus base-registered removals as ordinary zero-comparator policy changes.

Consequently, derived projections regenerated by the base engine are not misclassified as unregistered files, while moved editable policy still produces explicit `weaken-policy` obligations in addition to protected-move and trusted-component obligations. Invalid registries, undeclared files and uncovered changes continue to fail closed.
