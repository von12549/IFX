# Authorize the CP11r LayerGuard Post relocation

This authorization checkpoint binds the exact 13 per-file moves, trusted-component tuples and policy/config pointers of the frozen CP11r LayerGuard relocation candidate. It changes no Architecture implementation and is consumed by the following relocation checkpoint.
