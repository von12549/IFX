# CP11c profile registry preparation authorization (r2)

This authorization-only checkpoint permits the exact registry preparation candidate rebased after the profile repository-root bridge. It binds the temporary destination-directory registrations and the base-owned transition selector to their reviewed tuples.

The candidate must consume both authorizations in the same protected diff. The temporary directory entries authorize only the subsequent physical authority moves; they do not activate a new profile layout or change production inputs.
