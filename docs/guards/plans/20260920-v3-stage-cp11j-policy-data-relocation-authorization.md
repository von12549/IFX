# Authorize CP11j policy data relocation

This authorization-only checkpoint binds the five protected path moves, the exact trusted-component tuples and all zero-comparator policy/config changes of the frozen CP11j policy-data relocation candidate. It changes no runtime implementation or policy authority.
