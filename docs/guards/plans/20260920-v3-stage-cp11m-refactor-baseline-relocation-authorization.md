# CP11m — Authorize the refactor baseline relocation

This authorization checkpoint binds the prepared relocation candidate to fifteen non-overlapping path moves, the resulting `tcb.manifest` changes, and the three active manifest pointers that follow the new locations. Frozen baseline evidence is already classified as historical evidence by the preceding policy bridge and therefore creates no active-policy obligation.
