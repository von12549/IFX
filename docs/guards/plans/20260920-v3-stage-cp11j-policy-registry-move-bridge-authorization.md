# Authorize CP11j policy registry move bridge

This authorization-only checkpoint pre-authorizes the exact trusted-component tuples of the policy registry move bridge. It changes no runtime implementation, policy registry or authority registry.
