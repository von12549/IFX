# Authorize the CP11r LayerGuard layout bridge

This checkpoint binds the exact runner, package-test and decision-registry tuples of the frozen LayerGuard layout bridge. It changes no Architecture source and is consumed by the following bridge checkpoint.
