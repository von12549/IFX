# P11.5 cleanup-prep combined trusted-component authorization

This authorization-only checkpoint republishes `p11-cleanup-prep-trusted-base` after PR #97 revoked the two non-composable split records. It contains no implementation or policy change.

The record was generated by the base-owned authorization generator from base `3b0a2e8262cca6ab5d51cbdf16c0e1f16c244130` and candidate `531d027e239a86c1c6c04aeede10bbe8c4dbc420`. It binds exactly 19 TCB paths across all six changed components, including the trusted-base runner correction that scopes the one-time bridge to heads whose own changed set includes the CI declaration.

Two `Validate` differences are explicit. First, the base CI contract retains legacy Architecture cost-control entries while the candidate uses the canonical command. Second, the corrected bridge skips unrelated heads that do not change that declaration. The exact cleanup candidate still changes the declaration, consumes this record and the policy authorization, and therefore follows the narrowly authorized bridge path. All other parity modes remain exact.

After merge, PR #88 must merge the new base, consume this record together with `p11-cleanup-prep-policy`, pass the base-owned trusted Diff, pass trusted-component candidate verification, and pass all 13 required checks.
