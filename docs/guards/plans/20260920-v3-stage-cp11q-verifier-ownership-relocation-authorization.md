# Authorize the CP11q verifier ownership relocation

This authorization checkpoint binds the exact internal manifest verifier move, trusted-component tuples and policy/config pointers of the frozen CP11q relocation candidate. It changes no verifier implementation and is consumed by the following relocation checkpoint.
