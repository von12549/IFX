# CP11i documentation and example relocation authorization

This authorization-only checkpoint binds the three exact protected moves, the four affected trusted manifest tuples and the four semantic Stage-manifest documentation-path changes for CP11i. The relocation change consumes all five records exactly once.
