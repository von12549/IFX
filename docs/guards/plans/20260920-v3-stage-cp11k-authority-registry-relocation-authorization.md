# Authorize CP11k authority registry relocation

This authorization-only checkpoint binds the exact protected directory move, trusted-component tuples and registered policy changes of the frozen CP11k relocation candidate. It changes no runtime implementation, manifest or authority data.
