# CP11g Agent integration relocation authorization

This authorization-only checkpoint binds the exact legacy and destination tuples for moving the two IFX-specific Agent skills into `integrations/agents/`. The CP11g change must consume the record once without expanding its scope.
