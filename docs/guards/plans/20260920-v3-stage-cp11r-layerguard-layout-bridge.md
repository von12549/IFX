# CP11r — LayerGuard Post-layout bridge

This checkpoint makes the base-owned Architecture runner and isolated package test resolve exactly one legacy template or final Post-owned IFX Architecture source root. The complete existing test and scan corpus therefore runs unchanged on both sides of the 13-file relocation and rejects duplicate authorities.
