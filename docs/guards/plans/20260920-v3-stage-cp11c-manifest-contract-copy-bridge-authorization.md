# CP11c manifest fixture contract-copy bridge authorization

This authorization-only checkpoint permits the exact additive base-owned test change prepared for the CP11c manifest fixture bridge. The candidate must consume the record in the same protected diff; no production behavior change is authorized.
